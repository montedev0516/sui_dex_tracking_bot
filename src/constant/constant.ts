const startCommand = {
    command: 'start',
    description: 'Start bot for getting buy event',
  };
  
  const stopCommand = {
    command: 'stop',
    description: 'Stop bot for getting buy event',
  };
  
  export const commands = [startCommand, stopCommand];
  